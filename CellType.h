#ifndef _CELL_TYPE_H
#define _CELL_TYPE_H

/// \brief Enum class containing the possible cell types
enum class Type { EMPTY, INTEGER, DOUBLE, STRING, FORMULA, UNKNOWN };

#endif // !_CELL_TYPE_H
